MEMORY {
	RAM:  start = $200, size = $800;
	ROM:  start = $8000, size = $8000;
}

SEGMENTS {
	RAM_CODE: load = RAM, type = rw, start = $200;
	ROM_CODE: load = ROM, type = ro, start = $FD00;
	VECTORS: load = ROM, type = ro, start = $FFFA;
}
